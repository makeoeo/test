<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna_replyMapper">

	<!-- 댓글 조회 -->
		<select id="readReply" resultType="kr.lsh.vo.QnaBoardReplyVO">
	    	SELECT
	    		  RNO, CONTENT, WRITER, REGDATE
	    	 FROM QNA_REPLY
	    	WHERE BNO = #{bno}
		</select>
	<!-- 댓글작성 -->
		<insert id="writeReply">
			INSERT INTO QNA_REPLY(
				BNO
				, RNO
				, CONTENT
				, WRITER
			)
			VALUES(    #{bno}
				, QNA_REPLY_SEQ.NEXTVAL
				, #{content}
				, #{writer}	)
		</insert>
	<!-- 댓글 수정 -->
		<update id="updateReply" parameterType="kr.lsh.vo.QnaBoardReplyVO">
			UPDATE QNA_REPLY SET CONTENT = #{content}
			WHERE RNO = #{rno}
		</update>
	
	<!-- 댓글 삭제 -->
		<delete id="deleteReply" parameterType="kr.lsh.vo.QnaBoardReplyVO">
			DELETE FROM QNA_REPLY
			WHERE RNO = #{rno}
		</delete>
		
	<!-- 선택된 댓글 조회 -->
		<select id="selectReply" resultType="kr.lsh.vo.QnaBoardReplyVO">
			SELECT
					BNO
				  , RNO
				  , CONTENT
				  , WRITER
				  , REGDATE
			  FROM QNA_REPLY
			 WHERE RNO = #{rno}
	</select>
</mapper>